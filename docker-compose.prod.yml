services:
  nginx:
    image: nginx:alpine
    restart: unless-stopped
    # Le proxy déjà en place sur le serveur (ports 80/443 + certbot) route vers ce port loopback
    ports:
      - "0.0.0.0:${NGINX_PROD_PORT:-8095}:80"
    volumes:
      - ./nginx/conf.d.prod:/etc/nginx/conf.d:ro
    depends_on:
      - cms
      - frontend
    networks:
      - app_network

  cms:
    image: ghcr.io/${GHCR_OWNER:-idefix38}/jm-courtois-cms:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: ./cms/.env.production
    environment:
      - DATABASE_HOST=postgres
      - DATABASE_PORT=5432
      - DATABASE_NAME=${POSTGRES_DB:-jm-courtois}
      - DATABASE_USERNAME=${POSTGRES_USER:-user}
      - DATABASE_PASSWORD=${POSTGRES_PASSWORD:-password}
    volumes:
      - cms-uploads:/usr/app/public/uploads
    depends_on:
      - postgres
    networks:
      - app_network
      - postgres-network

  frontend:
    image: ghcr.io/${GHCR_OWNER:-idefix38}/jm-courtois-frontend:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: ./frontend/.env.production
    depends_on:
      - cms
    networks:
      - app_network

  postgres:
    image: postgres:17
    restart: unless-stopped
    env_file: .env.production
    volumes:
      - postgres-prod-data:/var/lib/postgresql/data
    networks:
      - postgres-network

volumes:
  postgres-prod-data:
  cms-uploads:

networks:
  app_network:
    driver: bridge
  postgres-network:
    driver: bridge

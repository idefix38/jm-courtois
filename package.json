{
  "name": "jm-courtois",
  "private": true,
  "scripts": {
    "dev": "docker compose -f docker-compose.yml up --build",
    "prod": "docker compose --env-file .env.production -f docker-compose.prod.yml up -d --pull always"
  }
}
